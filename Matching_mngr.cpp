#include "Matching_mngr.h"

match* Matching_mngr::generateMatching(block_t* , unsigned int len, unsigned int numThreads)
{
    return nullptr;
} //function to create threads, collect and order their work

match* Matching_mngr::identifyMatches(block_t , unsigned int len)
{
    return nullptr;
} //function to pass to threads, identifies Matches within a block
